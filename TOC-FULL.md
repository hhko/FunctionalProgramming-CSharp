# C# 함수형 프로그래밍 — 전체 세부 목차 (Part 1 ~ 12 / Ch01 ~ 42)

## Part 1 — Foundations (함수형 사고와 기계장치)

### 1장. 함수형 사고로의 전환 (두 평행 세계의 도입)
- **목표** — 이 장을 마치면 명령형 / 객체 지향 / 함수형 세 패러다임을 능력이 어디 사는가의 한 차원으로 비교하고, 함수형의 본질을 합성 가능한 Elevated World 로 lift 라는 한 문장으로 설명할 수 있습니다. Normal World 와 Elevated World 두 평행 세계, 그…

  - 1.1 1장에서 다루는 것 — 두 평행 세계 도입
  - 1.2 명령형 — 절차의 나열
    - 1.2.1 명령형의 발상
    - 1.2.2 명령형의 강점
    - 1.2.3 명령형의 약점 — 상태가 흩어집니다
    - 1.2.4 명령형 + 동시성 = 경쟁 조건
    - 1.2.5 명령형이 여전히 강한 곳
  - 1.3 객체 지향 — 능력의 캡슐화
    - 1.3.1 객체 지향의 등장 — 명령형의 한계를 푸는 시도
    - 1.3.2 객체 지향의 4 대 원칙
    - 1.3.3 객체 지향의 강점 — 도메인 모델링
    - 1.3.4 객체 지향의 약점 — 능력이 객체에 묶입니다
    - 1.3.5 객체 지향 + 변경 = 명령형의 비용이 객체 안으로
  - 1.4 함수형 — 함수의 합성
    - 1.4.1 함수형의 출발점 — 변경이 없는 세계관
    - 1.4.2 작은 차이가 만드는 큰 결과
    - 1.4.3 함수형의 세 결합 — 불변·합성·추상
    - 1.4.4 함수형의 표현 가능성 — 효과를 값으로
    - 1.4.5 같은 문제, 세 패러다임의 풀이
  - 1.5 능력은 어디 사는가 — 한 차원 비교
    - 1.5.1 한 차원 압축
    - 1.5.2 능력의 위치 — 시각적 비교
    - 1.5.3 데이터의 변화 — 시각적 비교
    - 1.5.4 세 패러다임의 누적
  - 1.6 두 평행 세계 — 함수형의 무대
    - 1.6.1 함수형의 본질 — 합성 가능한 Elevated World 로 lift
    - 1.6.2 Normal World — 일상의 C# 값과 함수
    - 1.6.3 Elevated World — 효과를 인코딩한 값들
    - 1.6.4 함수도 두 세계에 산다
    - 1.6.5 표기법 정리 — `a → b` 와 `E<a>` 읽는 법
    - 1.6.6 비유의 한계 — 비유 vs 시그니처
  - 1.7 두 세계를 오가는 4 가지 함수 + 능력의 자리
    - 1.7.1 4 가지 함수 유형 — 네 자리 + trait 매핑
    - 1.7.2 trait 부착 — 시민이 능력을 얻는 메커니즘
    - 1.7.3 `a → b` 평범한 합성
    - 1.7.4 `a → E<b>` 월드 교차 함수
    - 1.7.5 `E<a> → b` 끌어내림
    - 1.7.6 `E<a> → E<b>` 완전히 끌어올린 함수
    - 1.7.7 네 자리의 종합 — 자연 합성 vs 합성 되살리기
    - 1.7.8 한 LINQ 사슬로 기초 어휘 통합 — 함수형 풀이 다시 읽기
  - 1.8 기초의 모든 추상을 4 가지 함수 유형에 두기
  - 1.9 Q&A — 자기 점검
  - 1.10 요약
  - 1.11 다음 장으로

### 2장. Higher Kinds (수많은 Elevated World 를 하나의 어휘로)
- **목표** — 이 장을 마치면 같은 `Map` 을 컨테이너마다 베끼는 N×M 불편의 정체를, type constructor 를 매개변수로 못 받는 C# 의 Order 2 (HKT) 미지원이라는 한계로 짚어 시그니처로 설명할 수 있습니다. 또한 `K<in F, A>` 마커와 self-bound, sta…

  - 2.1 이 장에서 다루는 것 — 한 능력을 모든 컨테이너에
  - 2.2 왜 필요한가 — 같은 Map 을 N 번 베끼는 코드
    - 2.2.1 첫 컨테이너 — 잘 동작하는 Map
    - 2.2.2 두 번째, 세 번째 — 복사·붙여넣기가 시작됩니다
    - 2.2.3 동료가 능력을 하나 추가하면 — N×M 격자
  - 2.3 첫 시도와 좌절 — C# 으로 직접 써 보기
  - 2.4 첫 번째 우회 — `K<in F, A>` 마커 인터페이스
    - 2.4.1 한 줄 코드
    - 2.4.2 발상 — 막힌 자리를 한 단계 내려 되찾기
  - 2.5 두 번째·세 번째 우회 — self-bound 제약과 static abstract 멤버
    - 2.5.1 self-bound (`where F : Functor<F>`)
    - 2.5.2 static abstract (타입의 정적 멤버로서의 동작)
  - 2.6 가상 코드를 `K<F, A>` 로 재작성 — 최종 돌파
  - 2.7 모든 trait 가 따르는 공통 모양
  - 2.8 `MyList` 진화 — 예제 (세 약점의 해결)
    - 2.8.1 Before — `K<F, A>` 우회 없이
    - 2.8.2 After — 3-tuple 도입
    - 2.8.3 자료 / 태그 / trait — 책임 세 조각 (+ K 묶는 신호)
    - 2.8.4 호출 모양 — 정적 자리에서 부르기
    - 2.8.5 세 약점이 어떻게 해결되는가
    - 2.8.6 다운캐스트 `(MyList<A>)fa` — 왜 안전한가
  - 2.9 어떤 Functor 든 받는 일반 함수
  - 2.10 Elevated World 어휘로 다시 읽기
  - 2.11 깊이 보기 — 한계의 정식 이름 (선택적 심화)
    - 2.11.1 값 차원의 Order 단계 (Order 0 / 1 / 2)
    - 2.11.2 타입 차원의 kind 와 C# 의 한계
    - 2.11.3 Order 0 — 완성 타입 (C# 완전 제공)
    - 2.11.4 Order 1 — type constructor (C# 정의만 제공)
    - 2.11.5 Order 2 — HKT (kind 3 단계 도출)
    - 2.11.6 값 생성자 vs 타입 생성자 — 통합 비교
    - 2.11.7 C# 의 한계 — 두 에러 메시지와 다른 언어
  - 2.12 Q&A — 자기 점검
  - 2.13 요약
  - 2.14 다음 장으로

### 3 장. Monoid / Semigroup (Normal World 의 결합)
- **목표** — 이 장을 읽고 나면 Normal World 에서 같은 타입의 값 둘을 한 값으로 합치는 **결합 (Monoid)** 을 trait 으로 정의하고, 결합 법칙과 항등 법칙 두 약속으로 진짜 Monoid 를 판별할 수 있습니다. 같은 누적 코드를 도메인마다 베끼고 빈 입력의 답이 사람마다…

  - 3.1 이 장에서 다루는 것 — Normal World 의 결합
  - 3.2 왜 필요한가 — 같은 누적을 도메인마다 베끼는 코드
    - 3.2.1 한 도메인 — 잘 동작하는 누적
    - 3.2.2 같은 패턴이 다섯 번째 — 복사의 누적
    - 3.2.3 빈 입력의 어긋남 — 약속이 코드에 없을 때
  - 3.3 함수형의 발상 — 결합을 trait 으로
  - 3.4 결합의 약속 — Semigroup
    - 3.4.1 결합 연산이란 무엇인가
    - 3.4.2 결합 법칙
    - 3.4.3 Semigroup 의 한계 — 빈 경우
  - 3.5 Monoid — 항등원 도입
    - 3.5.1 항등원의 정의
    - 3.5.2 항등 법칙
    - 3.5.3 빈 입력도 안전하게 처리
  - 3.6 더 많은 Monoid — Sum / Product / Concat / Boolean
    - 3.6.1 Sum — 덧셈
    - 3.6.2 Product — 곱셈
    - 3.6.3 Concat — 문자열 이어붙이기
    - 3.6.4 Boolean — AND / OR
    - 3.6.5 같은 타입, 다른 Monoid — 어떻게 구분하나
  - 3.7 두 법칙 검증 — 결합 + 항등
  - 3.8 직접 구현 — Monoid trait 의 두 멤버
    - 3.8.1 Semigroup trait 정의
    - 3.8.2 Monoid trait 정의 — Semigroup 상속
    - 3.8.3 `Combine` 은 instance, `Empty` 는 `static abstract` — 의미가 어법을 결정합니다
  - 3.9 일반 함수 — `Monoid.combine` (Foldable 의 디딤돌)
  - 3.10 (선택적 심화) 4 장으로 가기 전 — Order, `K<F, A>`, 구현 정합
    - 3.10.1 Order 0 vs Order 1 — Monoid 의 자리
    - 3.10.2 `K<F, A>` 마커가 왜 없는가
    - 3.10.3 왜 Order 0 은 instance, Order 1 은 `static abstract` 인가
    - 3.10.4 구체 타입의 `+` 직접 호출 — extension static operator
    - 3.10.5 비 Monoid 를 Monoid 로 만드는 세 패턴
    - 3.10.6 법칙은 모든 입력의 약속 — property-based testing 의 씨앗
  - 3.11 Monoid 가 기초 / 4 부 / 5 부에서 다시 등장하는 자리
    - 3.11.1 8 장 Validation — 오류 누적 (기초 안)
    - 3.11.2 5 부 Writer — 로그 누적 (5 부)
    - 3.11.3 4 부 `SemigroupK` / `MonoidK` — Elevated World 의 결합
    - 3.11.4 마무리 한 줄
  - 3.12 Q&A — 자기 점검
  - 3.13 요약
  - 3.14 다음 장으로

## Part 2 — Core Traits (네 자리의 다리)

### 4장. Functor / `map` (가장 단순한 끌어올림)
- **목표** — 이 장을 읽고 나면 Functor 의 한 줄 정의 `(a → b) → (E<a> → E<b>)` 를 시그니처로 적고, `Functor<F>` trait 을 3-tuple 패턴으로 새 자료 타입에 부착하며, 두 법칙 (항등 / 합성) 을 코드로 검증할 수 있습니다. 이 장은 기초의 첫 정…

  - 4.1 `E<a> → E<b>` 유형 함수의 일반화 — 목적
    - 4.1.1 왜 필요한가 — 컨테이너마다 다시 쓰는 변환
    - 4.1.2 1장 4 가지 함수 유형의 `E<a> → E<b>` 자리
    - 4.1.3 4 가지 함수 유형 — 시그니처와 기초 매핑
    - 4.1.4 Lifting 의 3 종류
  - 4.2 `Functor<F>` trait 시그니처 — 기능
    - 4.2.1 세 줄의 정의
    - 4.2.2 시그니처가 강제하는 것 / 못 하는 것
    - 4.2.3 `map` 의 별명들
    - 4.2.4 변성 (variance) — `K<in F, A>`
    - 4.2.5 이 장의 코드 구조
  - 4.3 첫 인스턴스 — MyList Functor (예제)
    - 4.3.1 3-tuple 패턴으로 부착
    - 4.3.2 세 부분의 책임
    - 4.3.3 코드 walkthrough — 두 줄 분해
    - 4.3.4 호출 모양 — 세 가지 어법
    - 4.3.5 흔한 함정 — 다운캐스트의 안전성
  - 4.4 두 번째 인스턴스 — MyMaybe Functor (예제)
    - 4.4.1 완전히 다른 자료 구조에 같은 trait
    - 4.4.2 두 case 의 시그니처
    - 4.4.3 호출 모양 — 세 가지 어법
    - 4.4.4 MyList 와 MyMaybe 의 Map 비교
    - 4.4.5 문자열 파싱 결과의 변환 예제
    - 4.4.6 더 많은 Functor 인스턴스
  - 4.5 어떤 Functor 든 받는 일반 함수 — 예제
    - 4.5.1 trait 의 결정적 가치
    - 4.5.2 호출 모양
    - 4.5.3 일반 함수의 합성
    - 4.5.4 공짜 함수의 누적
  - 4.6 Functor 의 두 법칙 — 기능 (목적의 보장)
    - 4.6.1 시그니처만으로는 부족합니다
    - 4.6.2 첫 번째 법칙 — 항등 (Identity Law)
    - 4.6.3 두 번째 법칙 — 합성 (Composition Law)
    - 4.6.4 코드로 검증
    - 4.6.5 두 법칙이 함께 필요한 이유
  - 4.7 Functor 와 함수 합성 — 기능 (합성 정합)
    - 4.7.1 순수 함수 합성과 Functor 합성
    - 4.7.2 두 길의 정렬 그림
    - 4.7.3 합성 법칙의 실용적 가치
  - 4.8 Functor 가 아닌 변환 — 예제 (경계)
    - 4.8.1 시그니처로 보는 Functor / 비-Functor
    - 4.8.2 각자 다른 trait 의 자리
    - 4.8.3 흔한 함정 두 개
    - 4.8.4 Functor 의 제한이 가치
  - 4.9 직접 해보기 — 챌린지
    - 4.9.1 `Tree<A>` Functor 부착 + 두 법칙 검증 챌린지
    - 4.9.2 Functor 인지 아닌지 분류하기 챌린지
    - 4.9.3 더 깊은 챌린지 — Const, Pair
    - 4.9.4 두 챌린지가 노리는 능력
  - 4.10 Elevated World 어휘로 다시 읽기
  - 4.11 Q&A — 자기 점검
  - 4.12 요약
  - 4.13 다음 장으로 — 마무리 (5장 Applicative 다리)

### 5장. Applicative / `pure` + `apply` (다인자 함수의 끌어올림)
- **목표** — 이 장을 읽고 나면 4장 `map` 의 1인자 한계를 시그니처로 진단하고, `Pure` 와 `Apply` 두 멤버 위에서 `Lift2` / `Lift3` / `Lift4` 로 다인자 Normal 함수를 모양 그대로 Elevated World 로 끌어올릴 수 있습니다. 기초 흐름에서 Ap…

  - 5.1 `map` 만으로는 풀 수 없는 다인자 함수 — 목적
    - 5.1.1 4 가지 함수 유형의 `E<a> → E<b>` 가 다인자로 넓어지는 자리
    - 5.1.2 갇힌 함수를 풀어낼 새 도구 — `Apply` 의 시그니처 유도
    - 5.1.3 4 가지 함수 유형 — 시그니처와 기초 매핑
    - 5.1.4 이 장의 목표
  - 5.2 `Applicative<F>` trait 시그니처 — 기능 (Pure + Apply)
    - 5.2.1 두 멤버 + Functor 상속
    - 5.2.2 `Pure` — 가장 단순한 사다리
    - 5.2.3 `Apply` — 함수도 컨테이너 안에
    - 5.2.4 `Map` ≡ `Apply` ∘ `Pure` — Functor 가 자연스럽게 함의됩니다
    - 5.2.5 시그니처가 강제하는 것 / 못 하는 것
    - 5.2.6 이 장의 코드 구조
  - 5.3 첫 인스턴스 — MyMaybe Applicative (예제)
    - 5.3.1 3-tuple 패턴으로 부착
    - 5.3.2 세 부분의 책임
    - 5.3.3 코드 walkthrough — Apply 의 분기
    - 5.3.4 호출 모양 — 세 가지 어법
    - 5.3.5 단락 동작 — 한 측이 Nothing 이면 결과 Nothing
  - 5.4 두 번째 인스턴스 — MyValidation Applicative (예제)
    - 5.4.1 같은 trait 시그니처, 다른 결합 동작
    - 5.4.2 Apply 의 네 분기
    - 5.4.3 호출 모양 — 4 개 에러 한꺼번에
    - 5.4.4 두 인스턴스의 Apply 비교
    - 5.4.5 회원가입 폼 검증 예제 — 세 책임 분리의 N 인자 확장
  - 5.5 어떤 Applicative 든 받는 일반 함수 — 예제 (Curry + Lift)
    - 5.5.1 Curry — 다인자를 한 인자씩
    - 5.5.2 `Lift2` — `Curry → Pure → 2 × Apply`
    - 5.5.3 `Lift3 / Lift4` — 같은 패턴 N 회 반복
    - 5.5.4 Curried 형태 — 함수 자체를 돌려주는 모양
    - 5.5.5 `lift1 = map` — 같은 함수의 두 어휘
    - 5.5.6 trait 의 결정적 가치 — 일반 함수의 누적
  - 5.6 Applicative 의 다섯 법칙 — 기능 (목적의 보장)
    - 5.6.1 시그니처만으로는 부족합니다
    - 5.6.2 첫 번째 법칙 — 항등 (Identity Law)
    - 5.6.3 두 번째 법칙 — 동형 사상 (Homomorphism Law)
    - 5.6.4 세 번째 법칙 — 교환 (Interchange Law)
    - 5.6.5 네 번째 법칙 — 합성 (Composition Law)
    - 5.6.6 다섯 번째 법칙 — Functor 정합 (Functor Consistency Law)
    - 5.6.7 코드로 검증
    - 5.6.8 가짜 Applicative — 시그니처를 통과해도 법칙을 어기는 자리
  - 5.7 Functor 와 Applicative — 기능 (상속 정합)
    - 5.7.1 상속 사슬의 의미
    - 5.7.2 시그니처 평행 — Map / Pure / Apply
    - 5.7.3 도구 분류표
  - 5.8 Applicative 가 아닌 변환 — 예제 (경계)
    - 5.8.1 시그니처로 본 Applicative / 비-Applicative
    - 5.8.2 다른 trait 의 자리
    - 5.8.3 흔한 함정 두 개
  - 5.9 직접 해보기 — 챌린지
    - 5.9.1 `MyList Applicative` (cartesian 결합) 부착 + 항등 법칙 검증
    - 5.9.2 `Lift4` 직접 작성 + 회원가입 폼 누적
    - 5.9.3 더 깊은 챌린지 — 다음 세 가지 중 하나
  - 5.10 Elevated World 어휘로 다시 읽기
  - 5.11 Q&A — 자기 점검
  - 5.12 요약
  - 5.13 다음 장으로 — 마무리 (6장 Foldable 다리)

### 6장. Foldable / `fold` (Elevated 의 구조를 Normal 한 값으로 끌어내림)
- **목표** — 이 장을 마치면 Elevated World 의 컨테이너 `E<a>` 를 Normal World 의 한 값 `b` 로 끌어내리는 `fold` 를 시그니처로 적고, 새 자료 타입에 3-tuple 패턴으로 `Foldable<F>` 를 직접 부착할 수 있습니다. 4장 `map` 이 `E<a>…

  - 6.1 `E<a> → b` 유형 함수의 일반화 — 목적
    - 6.1.1 fold 의 mental model — 함수형의 for-loop
    - 6.1.2 1장 4 가지 함수 유형의 `E<a> → b` 자리
    - 6.1.3 4 가지 함수 유형 — 시그니처와 기초 매핑
    - 6.1.4 Lifting 의 반대 방향 — Lowering
  - 6.2 `Foldable<F>` trait 시그니처 — 기능
    - 6.2.1 두 줄의 정의
    - 6.2.2 두 abstract 의 차이 — `FoldRight` vs `FoldLeft`
    - 6.2.3 시그니처가 강제하는 것 / 못 하는 것
    - 6.2.4 `fold` 의 별명들
    - 6.2.5 변성 (variance) — `K<in F, A>`
    - 6.2.6 이 장의 코드 구조
  - 6.3 첫 인스턴스 — MyList Foldable (예제)
    - 6.3.1 3-tuple 패턴으로 부착
    - 6.3.2 세 부분의 책임
    - 6.3.3 코드 walkthrough — 두 방향 분해
    - 6.3.4 호출 모양 — 세 가지 어법
    - 6.3.5 흔한 함정 — `FoldRight` 와 `FoldLeft` 중 무엇을 쓰나
  - 6.4 두 번째 인스턴스 — MyMaybe Foldable (예제)
    - 6.4.1 완전히 다른 자료 구조에 같은 trait
    - 6.4.2 3-tuple 다이어그램 — MyMaybe Foldable
    - 6.4.3 두 case 의 시그니처
    - 6.4.4 호출 모양 — 세 가지 어법
    - 6.4.5 MyList 와 MyMaybe 의 Fold 비교
    - 6.4.6 문자열 파싱 결과의 합산 예제
    - 6.4.7 더 많은 Foldable 인스턴스
  - 6.5 어떤 Foldable 든 받는 일반 함수 — 예제
    - 6.5.1 trait 의 결정적 가치
    - 6.5.2 호출 모양
    - 6.5.3 일반 함수의 합성
  - 6.6 Foldable 의 일관성 법칙 — 기능 (목적의 보장)
    - 6.6.1 시그니처만으로는 부족합니다
    - 6.6.2 첫 번째 법칙 — 결과 일관성 (Consistency Law)
    - 6.6.3 두 번째 법칙 — 부수 효과 없음 (Purity Law)
    - 6.6.4 코드로 검증
    - 6.6.5 두 법칙이 함께 필요한 이유
  - 6.7 Foldable 의 자유 함수 누적 — 기능 (abstract 2 개에 산출 수십)
    - 6.7.1 trait 의 결정적 이득
    - 6.7.2 자유 함수 카탈로그
    - 6.7.3 자료 구조의 무관성
    - 6.7.4 기초의 자유 함수 누적
    - 6.7.5 LanguageExt v5 의 abstract 선택 — FoldWhile 로 일반화
  - 6.8 Foldable 가 아닌 변환 — 예제 (경계)
    - 6.8.1 시그니처로 보는 Foldable / 비-Foldable
    - 6.8.2 각자 다른 trait 의 자리
    - 6.8.3 흔한 함정 두 개
    - 6.8.4 Foldable 의 제한이 가치
  - 6.9 직접 해보기 — 챌린지
    - 6.9.1 `Tree<A>` Foldable 부착 + 일관성 검증 챌린지
    - 6.9.2 Foldable 인지 아닌지 분류하기 챌린지
    - 6.9.3 더 깊은 챌린지 — Const, Pair (선택)
    - 6.9.4 챌린지의 본연의 목표
  - 6.10 Elevated World 어휘로 다시 읽기
  - 6.11 Q&A — 자기 점검
  - 6.12 요약
  - 6.13 다음 장으로 — 마무리 (7장 Monad 다리)

### 7장. Monad / `bind` (`a → E<b>` 함수의 합성 되살리기)
- **목표** — 이 장을 읽고 나면 출력 타입만 Elevated 인 함수 `a → E<b>` 가 왜 직접 이어지지 않는지 시그니처로 설명하고, Applicative 에 멤버 단 하나 (`Bind`) 를 더해 그 합성을 Elevated World 안에서 되살릴 수 있습니다. 끌어올림 (`map`) 과 끌…

  - 7.1 `a → E<b>` 유형 함수의 합성 — 목적
    - 7.1.1 왜 필요한가 — World-crossing 함수는 직접 이어지지 않습니다
    - 7.1.2 1장 4 가지 함수 유형의 `a → E<b>` 자리
    - 7.1.3 4 가지 함수 유형 — 시그니처와 기초 매핑
    - 7.1.4 대각선을 수평으로 — 왜 한 칸 어긋나는가
  - 7.2 `Monad<M>` trait 시그니처 — 기능 (Bind)
    - 7.2.1 Bind 한 줄
    - 7.2.2 trait 정의 — Applicative 에 멤버 하나 추가
    - 7.2.3 Monad ⊃ Applicative ⊃ Functor — 코드로 닫힙니다
  - 7.3 Monad 부착 — `MyMaybe` 의 단락, `MyList` 의 비결정성
    - 7.3.1 3-tuple 패턴 + Bind 구현
    - 7.3.2 실전 — Bind 명시 호출
    - 7.3.3 단락 회로 — 한 단계만 실패해도 전체가 멈춥니다
    - 7.3.4 두 번째 자료 — `MyList` 의 비결정성
  - 7.4 Apply vs Bind — 독립 결합 vs 의존 결합
  - 7.5 Kleisli 합성 — `>=>` 로 합성 되살리기
    - 7.5.1 `Then` (`>=>`) — World-crossing 함수의 정식 합성
    - 7.5.2 `Id` = `Pure` — Kleisli 합성의 항등원
  - 7.6 LINQ — `bind` 의 C# 설탕
    - 7.6.1 `from-from-select` 가 곧 `Bind` 사슬입니다
    - 7.6.2 단락 회로도 LINQ 에서 그대로
    - 7.6.3 실전 — 의존 사슬: 조회 → 조회 → 출금 파이프라인
    - 7.6.4 같은 LINQ 가 List 에서는 모든 조합
    - 7.6.5 단락 추적 — 어느 단계에서 멈추는가
    - 7.6.6 `select` 은 `Map` 이다 — `SelectMany` = `Bind` + `Map`
  - 7.7 `flatten` — 중첩 `E<E<a>>` 평탄화
    - 7.7.1 Monad 를 정의하는 두 길 — `bind` 와 `flatten`
  - 7.8 어떤 Monad 든 받는 일반 함수 — 예제
    - 7.8.1 trait 의 결정적 가치
    - 7.8.2 공짜 함수의 누적
  - 7.9 Monad 의 세 법칙 — 기능 (목적의 보장)
    - 7.9.1 좌항등 / 우항등 / 결합
    - 7.9.2 코드로 검증 — `[Fact]` 로 적은 세 법칙
    - 7.9.3 시그니처가 약속하지 못하는 성질
    - 7.9.4 가짜 Monad — 값을 버리는 반례
    - 7.9.5 세 법칙은 각각 무슨 약속인가
  - 7.10 Monad 가 아닌 경계 — 예제
  - 7.11 직접 해보기 — 챌린지
    - 7.11.1 `Bind` 사슬을 시그니처 따라 추적하기
    - 7.11.2 `f >=> g` 를 `Bind` 만으로 짓고 좌항등 확인하기
    - 7.11.3 LINQ `from-from-select` 를 중첩 `Bind` 로 다시 쓰기
    - 7.11.4 다섯 시그니처가 어느 trait 자리인지 분류하기
    - 7.11.5 네 챌린지가 노리는 능력
  - 7.12 Elevated World 어휘로 다시 읽기
  - 7.13 Q&A — 자기 점검
  - 7.14 요약
  - 7.15 다음 장으로 — 마무리 (8장 Validation 다리)

## Part 3 — Composition (조합 · 실전 · 확장)

### 8장. Validation 실전 (applicative 누적 vs monadic 단락)
- **목표** — 기초에서 처음으로 이론을 실전에 붙입니다. 이 장을 읽고 나면 5장에서 만든 `MyValidation` 으로 회원가입 폼을 두 어법으로 풀어, applicative style 의 누적 (모든 오류를 한 번에) 과 monadic style 의 단락 (첫 오류에서 멈춤) 이 같은 입력에서…

  - 8.1 왜 필요한가 — 첫 실패에서 멈추는 폼
  - 8.2 도구 선택 — 5장의 `MyValidation`, 그리고 `Bind` 를 비운 까닭
    - 8.2.1 5장에서 만든 `MyValidation` 자리 복습
    - 8.2.2 Applicative 자리, Monad 자리 아님
  - 8.3 도메인 셋업 — 값 객체와 `DomainError`
  - 8.4 작은 검증자 — 한 함수 한 책임
  - 8.5 다인자 끌어올림 — `Curry → Pure → Apply` 사슬
  - 8.6 누적의 핵심 — `Apply` 의 `Invalid + Invalid` 분기
    - 8.6.1 오류 누적은 3장 Monoid 의 결합입니다
    - 8.6.2 오류가 쌓이는 과정 — 네 칸 모두 틀린 입력
  - 8.7 applicative style vs monadic style — 같은 도메인, 다른 의미
  - 8.8 에러에 컨텍스트 입히기 — `MapFail`
  - 8.9 실전 데모 — 4 사례
    - 8.9.1 또 다른 도메인 — 서버 설정 검증
    - 8.9.2 칸 하나에 규칙 여럿 — 같은 누적이 한 칸 안에서도
  - 8.10 직접 해보기 — 챌린지
    - 8.10.1 `Apply` 네 분기를 따라 한 건 누적 추적하기
    - 8.10.2 검증자를 하나 더 — 다섯 칸으로 늘리기
    - 8.10.3 `Bind` 를 정의하면 왜 누적이 사라지는가
    - 8.10.4 세 챌린지가 노리는 능력
  - 8.11 Elevated World 어휘로 다시 읽기
  - 8.12 Q&A — 자기 점검
  - 8.13 요약
  - 8.14 다음 장으로 — 마무리 (9장 Traversable 다리)

### 9장. Traversable / `traverse` (두 Elevated 세계의 층 순서 뒤집기)
- **목표** — 이 장을 읽고 나면 `List<E<a>>` 처럼 효과가 원소마다 흩어진 모양을 `E<List<a>>` 로 뒤집는 `traverse` 와 `sequence` 를 한 줄 시그니처로 적고, 그 층 swap 을 직접 작성한 코드로 추적할 수 있습니다. Traversable 이 4장 Functo…

  - 9.1 두 세계 비유로 본 Traversable — 목적
    - 9.1.1 왜 필요한가 — 효과가 원소마다 흩어진 자리
    - 9.1.2 1장 지도의 다섯 번째 이동 — 층 swap
  - 9.2 흔한 함정 — `Map` 만으로는 층이 안 뒤집힙니다
  - 9.3 목표 시그니처 — `List<E<a>> → E<List<a>>`
  - 9.4 `Traversable<T>` trait 시그니처 — 기능
  - 9.5 Traversable = Functor + Foldable + Applicative 의 합성
  - 9.6 `MyList` 에 Traversable 부착 — `Traverse` 구현
    - 9.6.1 구현 — `Pure` 로 시작해 `Apply` 로 조립
    - 9.6.2 단계별 실행 추적 — `Traverse([2, 4])` 가 `Just([2, 4])` 가 되기까지
  - 9.7 데모 — 전부 통과 vs 한 원소 단락
    - 9.7.1 안쪽 효과를 바꾸면 — MyValidation 으로 누적
  - 9.8 `sequence` 는 `traverse id` 입니다
  - 9.9 호출 어법과 어떤 Traversable 든 받는 일반 함수
    - 9.9.1 호출 어법 3 종
    - 9.9.2 어떤 Traversable 든 받는 일반 함수
    - 9.9.3 Traversable 이 아닌 경계
  - 9.10 traverse 의 법칙
    - 9.10.1 가짜 Traversable — 순서를 뒤집는 반례
  - 9.11 직접 해보기 — 챌린지
    - 9.11.1 `Traverse` 단락을 `Apply` 사슬로 추적하기
    - 9.11.2 안쪽 효과를 `MyValidation` 으로 — 단락에서 누적으로
    - 9.11.3 `sequence = traverse id` 확인하기
    - 9.11.4 세 챌린지가 노리는 능력
  - 9.12 Elevated World 어휘로 다시 읽기
  - 9.13 Q&A — 자기 점검
  - 9.14 요약
  - 9.15 다음 장으로 — 마무리 (10장 Bifunctor 다리)

### 10장. Bifunctor / Biapplicative / Bimonad (두 타입 인자 모두에 작용)
- **목표** — 이 장을 읽고 나면 타입 인자가 둘인 컨테이너 (`Either<L, R>`, `Pair<A, B>`) 에서 4장 Functor 의 한쪽만 변환하는 한계를 시그니처로 설명하고, `BiMap` 하나로 양쪽을 동시에 변환하는 Bifunctor 를 2-인자 마커 `K<F, L, A>` 위에 직…

  - 10.1 인자가 둘인 컨테이너의 양쪽 변환 — 목적
    - 10.1.1 왜 필요한가 — 한쪽만 바꾸는 보일러플레이트
    - 10.1.2 두 함수가 필요한 자리
  - 10.2 Bifunctor — 두 함수로 양쪽을 변환
  - 10.3 trait 직접 구현 — 2-인자 마커 `K<in F, L, A>`
  - 10.4 예제 — Either / Pair
    - 10.4.1 실전 — 계층 사이 오류 매핑
  - 10.5 가족 — Biapplicative / Bimonad
  - 10.6 Bifoldable — Foldable 의 2-인자 대칭
  - 10.7 두 법칙 — Functor 법칙의 두 인자 판
    - 10.7.1 가짜 Bifunctor — 두 자리를 뒤바꾸는 반례
    - 10.7.2 또 다른 가짜 — 항등은 지키고 합성을 깨는 반례
  - 10.8 직접 해보기 — 챌린지
    - 10.8.1 `Either` 의 오류 타입만 바꾸기 (`MapFirst`)
    - 10.8.2 `Pair` 양쪽 변환과 `MapSecond` = Functor 의 `map`
    - 10.8.3 가짜 `BogusBiMap` — 항등 법칙 위반
    - 10.8.4 가짜 `BogusBiMapTwice` — 항등은 통과, 합성은 위반
    - 10.8.5 네 챌린지가 노리는 능력
  - 10.9 Q&A — 자기 점검
  - 10.10 요약
  - 10.11 다음 장으로 — 마무리 (11장 NaturalTransformation 다리)

### 11장. NaturalTransformation (컨테이너 자체를 다른 컨테이너로)
- **목표** — 이 장을 읽고 나면 컨테이너 안의 값을 바꾸는 `map` 과 컨테이너 자체를 바꾸는 NaturalTransformation (`K<F, A> → K<G, A>`, 값 타입 `A` 는 유지) 을 시그니처로 구분하고, `Transform` 을 `Natural<F, G>` trait 으로 직…

  - 11.1 컨테이너 자체를 바꾸기 — 목적
    - 11.1.1 왜 필요한가 — 흩어지는 변환 함수
    - 11.1.2 컨테이너를 옮기고 싶을 때
  - 11.2 NaturalTransformation — 컨테이너를 옮기는 변환
  - 11.3 trait 직접 구현 — `Natural<F, G>`
  - 11.4 예제 — MyList ↔ MyMaybe
  - 11.5 자연성 법칙 — 변환과 map 의 순서 무관
    - 11.5.1 가짜 자연 변환 — 값을 들여다보는 반례
  - 11.6 sequence 는 NaturalTransformation 입니다
  - 11.7 직접 해보기 — 챌린지
    - 11.7.1 값은 그대로, 컨테이너만 옮기기
    - 11.7.2 자연성 법칙의 두 경로가 만남을 확인하기
    - 11.7.3 가짜 `BogusEvenHead` — 자연성 위반
    - 11.7.4 세 챌린지가 노리는 능력
  - 11.8 Q&A — 자기 점검
  - 11.9 요약
  - 11.10 기초를 마치며 — 다섯 trait 과 두 확장

## Part 4 — Collections (불변 컬렉션)

### 12 장. Sequences (실무 시퀀스에 부착하는 다섯 trait)
- **목표** — 이 장을 읽고 나면 기초에서 toy 타입 `MyList` 에 부착했던 다섯 trait (Functor / Applicative / Monad / Foldable / Traversable) 이 실무 lazy 시퀀스 `MySeq` 에 글자 그대로 다시 붙는다는 것을 직접 구현으로 확인하고,…

  - 12.1 이 장에서 다루는 것 — 기초 추상의 첫 실무 시민
  - 12.2 왜 필요한가 — toy 타입은 실무 자료 구조가 아니다
  - 12.3 MySeq — 계산을 미루는 lazy 시퀀스
    - 12.3.1 lazy 가 무한 시퀀스까지 다룬다
  - 12.4 다섯 trait 이 그대로 붙는다
    - 12.4.1 Functor — 값만 변환하되, 계산은 미룬다
    - 12.4.2 Applicative — Pure 와 Apply (데카르트 곱)
    - 12.4.3 Monad — Bind, 그리고 LINQ 의 정체
    - 12.4.4 Foldable — 일상의 함수들이 두 멤버 위에서 자란다
    - 12.4.5 Traversable — 모두 성공해야 성공 (traverse 와 sequence)
  - 12.5 표현이 달라도 같은 trait — cons 리스트 MyLst
  - 12.6 한 자료 타입에 두 Applicative — 데카르트 곱 vs zip
  - 12.7 직접 해보기 — 챌린지
  - 12.8 Elevated World 어휘로 다시 읽기
  - 12.9 Q&A — 자기 점검
  - 12.10 요약
  - 12.11 다음 장으로

### 13 장. Maps & Sets (trait 이 붙는 자리와 붙지 않는 자리)
- **목표** — 이 장을 읽고 나면 키-값 컨테이너 `MyMap` 에서 Functor 의 `map` 이 **값에만** 작용하고 키는 보존된다는 것을 직접 구현으로 확인하고, 집합 `MySet` 에는 `Foldable` 만 깔끔히 붙고 `Functor` 는 왜 경계 사례인지 두 가지 이유로 설명할 수 있…

  - 13.1 이 장에서 다루는 것 — 컨테이너마다 다른 trait 의 자리
  - 13.2 왜 필요한가 — 키-값과 집합은 시퀀스와 모양이 다르다
  - 13.3 MyMap — 값에만 작용하는 Functor
  - 13.4 MyMap Foldable — 값들이 한 값으로 자란다
  - 13.5 MySet — Foldable 만 붙고, Functor 는 경계 사례
  - 13.6 키 변환은 Functor 가 아니다 — 그리고 충돌을 다루는 법
  - 13.7 내부 표현은 달라도 같은 trait — 정렬 Map · HashMap · TrieMap
  - 13.8 Map 도 Traversable — 값들을 한꺼번에 검증
  - 13.9 직접 해보기 — 챌린지
  - 13.10 Elevated World 어휘로 다시 읽기
  - 13.11 Q&A — 자기 점검
  - 13.12 요약
  - 13.13 다음 장으로

### 14 장. Alternative & MonoidK (선택과 결합)
- **목표** — 이 장을 읽고 나면 기초에 없던 두 새 추상을 직접 구현할 수 있습니다. 여러 Elevated 후보 중 하나를 고르는 `Choose` (Choice), 두 Elevated 값을 합치는 `Combine` 과 그 항등원 `Empty` (SemigroupK / MonoidK) 입니다. `Co…

  - 14.1 이 장에서 다루는 것 — 기초에 없던 두 동사
  - 14.2 왜 필요한가 — "고르기" 와 "모으기" 가 막히는 자리
  - 14.3 SemigroupK / MonoidK — 기초 Monoid 의 Elevated 판
  - 14.4 Choice — 여러 후보 중 하나를 고른다
  - 14.5 같은 시그니처, 다른 의미 — Combine vs Choose
  - 14.6 우연히 같아지는 자리 — MyMaybe
  - 14.7 Alternative — Choice + Applicative + Empty
  - 14.8 option — 실패하면 기본값 (fallback)
  - 14.9 guard — 조건 필터의 토대
  - 14.10 세 법칙 — "첫 성공" 의 약속을 정한다
  - 14.11 Choose 의 실무 얼굴 — 파서 결합자
  - 14.12 직접 해보기 — 챌린지
  - 14.13 Elevated World 어휘로 다시 읽기
  - 14.14 Q&A — 자기 점검
  - 14.15 요약
  - 14.16 4부를 마치며

## Part 5 — Effect Monads (효과를 담는 모나드)

### 15장. Reader (환경 의존 효과)
- **목표** — 이 장을 마치면 설정이나 의존성 같은 환경을 가변 상태나 전역 변수 없이 순수 함수의 타입에 담아 다룰 수 있습니다. 지금까지 Elevated World 의 시민은 자료를 담는 컨테이너 (`Option` / `List` / `Validation`) 였지만, 5부의 시민은 함수 `Env…

  - 15.1 이 장에서 다루는 것 — 효과를 타입에 담는 첫걸음
  - 15.2 왜 필요한가 — 설정을 모든 함수에 넘기는 번거로움
  - 15.3 `Reader<Env, A>` — `Env → A` 의 끌어올림
  - 15.4 trait 부착 — `Map` · `Pure` · `Apply` · `Bind`
    - 15.4.1 `Bind` — 같은 환경을 두 단계에 흘려보냄
  - 15.5 `Readable<M, Env>` — 함수형 DI 의 세 동사
  - 15.6 실전 — `BudgetReader`
  - 15.7 `Local` — 국소 환경 변경
  - 15.9 직접 해보기
  - 15.10 Elevated World 어휘로 다시 읽기
  - 15.11 Q&A — 자기 점검
  - 15.12 요약
  - 15.13 다음 장으로

### 16장. State (상태 스레딩 효과)
- **목표** — 이 장을 마치면 카운터나 누적기처럼 단계마다 갱신되는 상태를 가변 필드나 전역 변수 없이 순수 함수의 타입에 담아 다룰 수 있습니다. 앞 장의 Reader 는 환경을 모든 단계에 흘리기만 했지만, State 는 한 발 더 나아가 상태를 읽고 또 씁니다. 5부의 시민이 함수 `Env →…

  - 16.1 이 장에서 다루는 것 — 읽기에서 읽고 쓰기로
  - 16.2 왜 필요한가 — 가변 카운터로 상태를 들고 다니는 번거로움
  - 16.3 `State<S, A>` — `S → (A, S)` 의 끌어올림
  - 16.4 trait 부착 — `Map` · `Pure` · `Apply` · `Bind`
    - 16.4.1 `Bind` — 새 상태를 다음 단계로 실어 나름
  - 16.5 `Stateful<M, S>` — 상태를 읽고 쓰는 네 동사
  - 16.6 실전 — 네 동사 한 흐름
    - 16.6.1 상태가 자료 구조일 때 — 스택
    - 16.6.2 임시 상태로 하위 계산 돌리기
  - 16.8 법칙 — Monad 세 법칙
  - 16.9 직접 해보기
  - 16.10 Elevated World 어휘로 다시 읽기
  - 16.11 Q&A — 자기 점검
  - 16.12 요약
  - 16.13 다음 장으로

### 17장. Writer (누적 출력 효과)
- **목표** — 이 장을 마치면 로그나 메트릭처럼 계산 단계마다 한쪽으로 쌓이는 출력을 가변 리스트나 전역 로거 없이 순수 함수의 타입에 담아 다룰 수 있습니다. 앞 장의 State 는 상태를 읽고 또 썼지만, Writer 는 상태를 되읽지 않고 결과에 출력을 곁들여 누적만 합니다. 그리고 5부의 시민…

  - 17.1 이 장에서 다루는 것 — 결과에 출력을 곁들여 누적하기
  - 17.2 왜 필요한가 — 로그를 전역·가변 리스트로 들고 다니는 번거로움
  - 17.3 `Writer<W, A>` — 쌍 `(A, W)` 의 끌어올림
  - 17.4 trait 부착 — `Map` · `Pure` · `Apply` · `Bind`
    - 17.4.1 `Bind` — 두 출력을 Monoid 로 누적
  - 17.5 `W` 는 왜 Monoid 인가 — 3장의 재방문
    - 17.5.1 같은 Writer, 다른 Monoid
  - 17.6 `Writable<M, W>` — `Tell` · `Listen` · `Pass`
    - 17.6.1 `censor` — `Pass` 를 감싼 검열 입구
  - 17.7 실전 — 계산 과정 로그 누적 (`TracedMath`)
  - 17.8 법칙 — Monad 세 법칙
  - 17.9 직접 해보기
  - 17.10 Elevated World 어휘로 다시 읽기
  - 17.11 Q&A — 자기 점검
  - 17.12 요약
  - 17.13 다음 장으로

### 18장. 왜 변환기가 필요한가 (단일 모나드의 한계)
- **목표** — 이 장을 마치면 모나드 하나로는 여러 효과를 동시에 담지 못한다는 한계를 코드로 직접 보일 수 있습니다. 15장부터 17장까지 환경 의존 · 상태 · 출력 누적이라는 세 효과를 각각 모나드로 담았고, 셋 다 `Bind` 와 LINQ 로 깔끔하게 합성됐습니다. 그런데 실전은 한 함수 안에…

  - 18.1 이 장에서 다루는 것 — 한 모나드로는 부족한 자리
  - 18.2 왜 필요한가 — 환경 의존과 실패가 한 함수에
  - 18.3 각각은 잘 합성된다 — 복습
  - 18.4 두 효과를 그냥 겹치면 — 쌓이는 `Option` 중첩
  - 18.5 손으로 짠 `ReaderOption` — 두 층을 직접 푸는 `Bind`
    - 18.5.1 `Bind` — env 층을 흘리고 Some·None 층을 분기
  - 18.6 lift — 안쪽 효과를 스택으로 끌어올리기
  - 18.7 법칙 — 손으로 짠 것도 진짜 모나드
  - 18.8 한계의 핵심 — 효과 쌍마다 다시 짜야 한다
  - 18.9 직접 해보기
  - 18.10 Elevated World 어휘로 다시 읽기
  - 18.11 Q&A — 자기 점검
  - 18.12 요약
  - 18.13 다음 부로 — 6부 모나드 변환기

## Part 6 — Monad Transformers (세계를 쌓다 — 모나드 변환기)

### 19장. 변환기 발상과 lift (첫 변환기 OptionT)
- **목표** — 이 장을 마치면 변환기 `T<M, A>` 가 한 효과 층과 빈 내부 모나드 `M` 으로 이루어진다는 것을, 첫 변환기 `OptionT<M, A>` 를 손에 쥐고 설명할 수 있습니다. 앞 장에서 우리는 `Reader` 와 `Option` 두 효과를 한 스택에 담으려고 `ReaderOpti…

  - 19.1 이 장에서 다루는 것 — 손작업을 자동으로
  - 19.2 왜 필요한가 — 효과 쌍마다 다시 짜는 `Bind`
  - 19.3 변환기의 발상 — 안쪽 모나드를 빈칸으로
  - 19.4 첫 변환기 `OptionT<M, A>` — 자료 한 줄 읽기
  - 19.5 두 층을 푸는 `Bind` — `switch` 한 자리가 `M.Bind` 로
  - 19.6 `MonadT` trait 과 lift — 내부 효과를 한 층 위로
  - 19.7 `OptionT<ManyF>` 데모 — 비결정성과 실패를 한 스택에
    - 19.7.1 예제 1 — lift 로 `Many` 를 한 층 위로
    - 19.7.2 예제 2 — 두 효과를 한 사슬에
  - 19.8 법칙 — 손으로 짠 변환기도 진짜 모나드
  - 19.9 직접 해보기
  - 19.10 Elevated World 어휘로 다시 읽기
  - 19.11 Q&A — 자기 점검
  - 19.12 요약
  - 19.13 다음 장으로

### 20장. ReaderT · StateT · WriterT (5부 단일 효과의 일반화)
- **목표** — 이 장을 마치면 5부에서 만난 단일 효과 모나드 셋 (Reader · State · Writer) 을 내부 모나드 `M` 위로 일반화한 변환기 `ReaderT<Env, M, A>` · `StateT<S, M, A>` · `WriterT<W, M, A>` 를 직접 구현으로 읽고, 18장에…

  - 20.1 이 장에서 다루는 것 — 단일 효과를 빈칸 위로
  - 20.2 왜 필요한가 — 같은 바깥 효과, 다른 안쪽 모나드
  - 20.3 `ReaderT<Env, M, A>` = `Env → M<A>`
  - 20.4 18장 `ReaderOption` 이 공짜로 — `ReaderT<int, OptionF>`
  - 20.5 `StateT<S, M, A>` = `S → M<(A, S)>`
  - 20.6 `StateT<List<int>, OptionF>` 데모 — 상태 + 실패가 한 스택에
  - 20.7 `WriterT<W, M, A>` — 로그 누적, 한 가지가 다르다
  - 20.8 trait 부착 — 5부의 동사가 변환기 위에서도 그대로
  - 20.9 법칙 — 변환기도 진짜 모나드
  - 20.10 직접 해보기
  - 20.11 Elevated World 어휘로 다시 읽기
  - 20.12 Q&A — 자기 점검
  - 20.13 요약
  - 20.14 다음 장으로 — `OptionT` · `EitherT`

### 21장. OptionT · EitherT (부재 · 오류를 내부 효과 위에)
- **목표** — 이 장을 마치면 실패를 내부 효과 위에 한 층으로 얹는 변환기를 직접 구현하고 읽을 수 있습니다. 19장에서 `OptionT<M, A>` 가 "없을 수 있음" 이라는 부재를 내부 모나드 `M` 위에 얹는 첫 변환기였고, 20장에서 `ReaderT` · `StateT` · `WriterT…

  - 21.1 이 장에서 다루는 것 — 부재 vs 오류
  - 21.2 왜 필요한가 — `None` 은 "없다" 만 말한다
  - 21.3 `EitherT<L, M, A> = M<Either<L, A>>` — 자료와 Bind
  - 21.4 `EitherT<string, ManyF>` 데모 — 갈래마다 이유 있는 실패
  - 21.5 스택 순서의 의미 — 어느 효과가 바깥인가
  - 21.6 OptionT 와의 관계 — `None` 은 이유 없는 `Left`
  - 21.7 `Lift` 와 `Pure` — 둘 다 `Right` 인데 자리가 다르다
  - 21.8 두 가지 단락과 실패 보존 — `Fail` 과 `Map`
  - 21.9 법칙 — 손으로 짠 것도 진짜 모나드
  - 21.10 직접 해보기
  - 21.11 Elevated World 어휘로 다시 읽기
  - 21.12 Q&A — 자기 점검
  - 21.13 요약
  - 21.14 다음 장으로

### 22장. MonadIO 와 LiftIO (IO 의 특수 lift, 7부로 가는 다리)
- **목표** — 이 장을 마치면 일반 `lift` 와 다른 `LiftIO` 라는 특수한 끌어올림 하나를 설명하고 직접 써 볼 수 있습니다. 19장부터 21장까지 변환기의 일반 `lift` 가 내부 모나드 계산을 바로 위 한 층으로 끌어올림을 봤습니다. 그런데 실전에서 가장 자주 쓰이는 효과는 부수 효과…

  - 22.1 이 장에서 다루는 것 — IO 의 특수 lift, 7부로 가는 다리
  - 22.2 왜 필요한가 — 깊은 스택의 안쪽 IO 를 한 층씩 올리는 번거로움
  - 22.3 IO<A> 한 줄 상기 — 부수 효과를 조립한 값
  - 22.4 MonadIO trait 과 LiftIO — 멤버 하나의 약속
  - 22.5 일반 lift 대 LiftIO — 한 층이냐 맨 안쪽이냐
  - 22.6 ReaderT<int, IOF> 데모 — LiftIO 로 IO 끌어올리기
    - 22.6.1 LiftIO = Lift(M.LiftIO(ma)) — 두 단계 추적
  - 22.7 이중 지연 — Run(env) 은 조립, io.Run() 은 실행
    - 22.7.1 첫 시점 — Run(env=6) 직후
    - 22.7.2 둘째 시점 — io.Run()
  - 22.8 v5 와의 정합 — 한 글자 제약의 차이
  - 22.9 Eff<RT, A> = ReaderT<RT, IO, A> 예감 — 7부로 가는 다리
  - 22.10 법칙 — IO 를 품은 스택도 진짜 모나드
  - 22.11 직접 해보기
  - 22.12 Elevated World 어휘로 다시 읽기
  - 22.13 Q&A — 자기 점검
  - 22.14 요약
  - 22.15 다음 부로 — 7부 효과 시스템

## Part 7 — Effect System (IO 와 효과 런타임)

### 23장. IO — 지연 효과의 인코딩 (DSL 노드 + 트램폴린)
- **목표** — 이 장을 마치면 `IO<A>` 가 부수 효과를 즉시 실행하지 않고 작은 노드 트리로 인코딩한 값임을 설명하고, 그 트리를 해석하는 트램폴린 인터프리터를 직접 읽을 수 있습니다. 22장에서 `IO<A>` 가 부수 효과를 곧장 실행하지 않고 조립만 해 둔 값 (thunk `() → A` 한…

  - 23.1 이 장에서 다루는 것 — 부수 효과를 값으로
  - 23.2 왜 필요한가 — thunk 한 개로는 깊은 Bind 가 스택을 터뜨린다
  - 23.3 IO 를 노드 트리로 — 세 노드의 인코딩
  - 23.4 트램폴린 인터프리터 — C# 재귀를 힙 위 스택으로
    - 23.4.1 손계산 — Pure(1).Bind(+1).Bind(×2) 를 한 단계씩
  - 23.5 스택 안전 — 십만 단계 Bind 가 평평하게 돈다
  - 23.6 Run 전 지연 — 조립 시점엔 부수 효과가 없다
  - 23.7 EnvIO 취소 — 인터프리터 루프 매 단계의 확인
  - 23.8 trait 부착과 법칙 — 손으로 짠 인터프리터도 진짜 모나드
  - 23.9 직접 해보기
  - 23.10 Elevated World 어휘로 다시 읽기
  - 23.11 Q&A — 자기 점검
  - 23.12 요약
  - 23.13 다음 장으로

### 24장. Error · Fin · Fallible (예외를 값으로)
- **목표** — 이 장을 마치면 함수형이 예외를 던지지 않고 값으로 다루는 방식을 설명하고 직접 구현할 수 있습니다. 23장에서 부수 효과를 `IO<A>` 라는 값으로 인코딩해 실행을 미뤘습니다. 그런데 부수 효과만큼 흔한 또 하나가 실패입니다. 파싱이 깨지고, 검증이 떨어지고, 파일이 없습니다. 명령…

  - 24.1 이 장에서 다루는 것 — 예외를 값으로
  - 24.2 왜 필요한가 — 예외는 흐름 바깥으로 튀고 시그니처에 안 드러난다
  - 24.3 Error — 오류를 구조화한 값
  - 24.4 Fin<A> = Succ ⊕ Fail — 성공과 실패를 한 값에
  - 24.5 Bind 단락 — Succ 잇고 Fail 단락
    - 24.5.1 단락을 손으로 따라가기
  - 24.6 Fallible trait — Fail 과 Catch
  - 24.7 Try — 예외를 Error 로 포획하는 World-crossing
  - 24.8 Final.Finally — try-finally 의 함수형 대응
  - 24.9 법칙 — Fin 도 진짜 모나드
  - 24.10 직접 해보기
  - 24.11 Elevated World 어휘로 다시 읽기
  - 24.12 Q&A — 자기 점검
  - 24.13 요약
  - 24.14 다음 장으로 — 25장 Eff (런타임 없는 효과)

### 25장. Eff — 런타임 없는 효과 (IO + 오류)
- **목표** — 이 장을 마치면 부수 효과와 실패를 한 타입에서 함께 다루는 효과 `Eff<A>` 를 설명하고 직접 조립해 볼 수 있습니다. 23장에서 `IO<A>` 가 부수 효과를 `Run` 까지 미뤄 둔 값임을 봤고, 24장에서 `Fin<A>` 와 `Error` 가 예외를 던지지 않고 성공·실패를…

  - 25.1 이 장에서 다루는 것 — IO 와 오류를 한 타입에
  - 25.2 왜 필요한가 — IO 와 오류를 매번 손으로 겹치는 번거로움
  - 25.3 Eff<A> = IO<Fin<A>> — 두 겹으로 겹친 효과
  - 25.4 지연과 오류 포획 — Run 전까지 미실행, 예외는 값으로
  - 25.5 실패 단락 — 첫 Fail 에서 이후를 건너뛴다
  - 25.6 Catch 폴백 — 실패를 받아 대체 효과로
  - 25.7 Choose 와 Finally — 효과 위의 고르기와 정리
  - 25.8 법칙 — 실패를 품은 효과도 진짜 모나드
  - 25.9 직접 해보기
  - 25.10 Elevated World 어휘로 다시 읽기
  - 25.11 Q&A — 자기 점검
  - 25.12 요약
  - 25.13 다음 장으로 — Eff<RT, A> = ReaderT<RT, IO, A>

### 26장. Eff<RT, A> = ReaderT<RT, IO, A> (런타임 주입과 Has DI)
- **목표** — 이 장을 마치면 7부 내내 예고된 실무 효과 타입 `Eff<RT, A>` 가 사실 6부에서 쌓은 `ReaderT<RT, IO, A>` 임을 직접 구현으로 확인하고, 런타임 `RT` 를 환경으로 주입해 의존성을 다루는 효과를 직접 짤 수 있습니다. 콘솔 출력이나 시간 조회 같은 능력을 코…

  - 26.1 이 장에서 다루는 것 — 7부의 정점, 능력을 주입받는 효과
  - 26.2 왜 필요한가 — 능력을 코드에 박으면 테스트가 막힙니다
  - 26.3 Eff<RT, A> = ReaderT<RT, IO, A> — 6부 변환기의 회수
  - 26.4 Has<RT, IConsole> — 컴파일러가 검증하는 능력 주입
    - 26.4.1 능력을 인터페이스로
    - 26.4.2 런타임이 능력을 가진다는 약속 — Has
    - 26.4.3 효과가 능력을 요구한다 — where 제약
  - 26.5 같은 코드, 두 런타임 — 주입만 바꾼다
    - 26.5.1 예제 1 — 테스트 런타임으로 결정적 실행
    - 26.5.2 예제 2 — 다른 입력의 런타임
  - 26.6 두 단계 Run — 조립과 실행
    - 26.6.1 첫 단계 — Run(rt) 은 IO 를 조립한다
    - 26.6.2 둘째 단계 — io.Run() 은 부수 효과를 일으킨다
  - 26.7 결정적 테스트 — 11부로 가는 다리
  - 26.8 법칙 — Eff<RT, A> 도 진짜 모나드
  - 26.9 v5 와의 정합 — Has 의 모양 차이
    - 26.9.1 정합하는 자리 — Eff = ReaderT 는 비유가 아니다
    - 26.9.2 갈라지는 자리 — Has 의 멤버
  - 26.10 Elevated World 어휘로 다시 읽기
  - 26.11 Q&A — 자기 점검
  - 26.12 요약
  - 26.13 직접 해보기
  - 26.14 다음 부로 — 8부 견고한 효과

## Part 8 — Robust Effects (견고한 효과)

### 27장. Schedule — 재시도와 반복 (정책을 값으로)
- **목표** — 이 장을 마치면 재시도와 반복의 정책을 비즈니스 로직에 흩지 않고 하나의 값으로 떼어낼 수 있습니다. 그 값이 `Schedule` 이고, "다음에 언제 다시 할지" 를 담은 (잠재적으로 무한한) `Duration` 스트림임을 직접 구현으로 확인합니다. 명령형의 재시도가 `for` 루프와…

  - 27.1 이 장에서 다루는 것 — 정책을 값으로
  - 27.2 왜 필요한가 — 명령형 재시도는 비즈니스 로직에 흩어집니다
  - 27.3 Schedule = Duration 스트림 — 정책을 펼치다
  - 27.4 union (`|`) 과 intersect (`&`) — 두 정책을 합치다
  - 27.5 retry 얹기 — 효과를 바꾸지 않고 정책만 더하다
  - 27.6 repeat — 성공을 반복하다
  - 27.7 구조 법칙 — union 과 intersect 의 의미를 굳히다
  - 27.8 Elevated World 어휘로 다시 읽기
  - 27.9 Q&A — 자기 점검
  - 27.10 요약
  - 27.11 직접 해보기
  - 27.12 다음 장으로 — 자원 수명 관리

### 28장. Resource & bracket (자원 수명, 예외에도 해제)
- **목표** — 이 장을 마치면 자원의 획득·사용·해제 세 단계를 `bracket` 이라는 한 값으로 묶어, 사용 도중 예외가 나도 해제가 반드시 실행되게 만드는 효과를 직접 짤 수 있습니다. 자원을 여럿 잡았을 때 나중에 연 것부터 먼저 닫는 역순 해제 (LIFO) 를 `Resources` 로 직접…

  - 28.1 이 장에서 다루는 것 — 반드시 닫히는 자원
  - 28.2 왜 필요한가 — 자원이 여럿이면 닫는 일이 흩어집니다
  - 28.3 bracket — 획득·사용·해제 세 단계를 한 값으로
  - 28.4 예외에도 해제 보장 — release 가 예외보다 먼저
  - 28.5 Resources — 여러 자원을 역순으로 닫기
  - 28.6 중첩 자원 — 안쪽이 먼저 닫힙니다
  - 28.7 EnvIO 가 자원을 운반한다 — 7부와 잇기
  - 28.8 보장 검증 — 세 가지 동작을 코드로
    - 28.8.1 v5 와의 정합 — bracket 의 속은 Finally 다
  - 28.9 Elevated World 어휘로 다시 읽기
  - 28.10 Q&A — 자기 점검
  - 28.11 요약
  - 28.12 직접 해보기
  - 28.13 다음 장으로

### 29장. Observability — Activity 와 분산 추적 (효과를 바꾸지 않고 관측)
- **목표** — 이 장을 마치면 운영 환경에서 "어느 단계가 느린가, 어디서 실패하나" 를 알려 주는 분산 추적 (distributed tracing) 을 효과 위에 직접 얹을 수 있습니다. 효과 구간을 span 으로 감싸면, 효과의 결과 값은 그대로이고 옆에 중첩 span 트리 (부모-자식 타이밍)…

  - 29.1 이 장에서 다루는 것 — 관측 가능한 효과
  - 29.2 왜 필요한가 — 관측 코드가 효과 본문에 흩어집니다
  - 29.3 Activity 와 span — 효과 구간을 감싸기
  - 29.4 중첩 span 트리 — 부모와 자식
  - 29.5 결과 불변 — 효과를 바꾸지 않는 횡단 관심사
  - 29.6 예외가 나도 닫히는 구간 — 28장 bracket 의 finally
  - 29.7 v5 와 OpenTelemetry 정합 — System.Diagnostics.Activity
    - 29.7.1 정합하는 자리 — span 으로 효과를 감싸는 발상
    - 29.7.2 현재 구간을 들고 다니는 방식 — 스택과 Reader 환경
    - 29.7.3 정직한 차이 — 리스너와 풍부한 span 데이터
  - 29.8 법칙 — 추적이 지키는 세 가지
  - 29.9 Elevated World 어휘로 다시 읽기
  - 29.10 Q&A — 자기 점검
  - 29.11 요약
  - 29.12 직접 해보기
  - 29.13 다음 부로 — 9부 동시성

## Part 9 — Concurrency (동시성)

### 30장. Atom — 락 없이 안전한 원자적 참조 (CAS 로 상태를 갈아끼우다)
- **목표** — 이 장을 마치면 여러 스레드가 공유하는 값을 락 한 줄 없이 안전하게 갱신하는 `Atom<A>` 를 직접 구현할 수 있습니다. 핵심은 갱신을 "값을 직접 +1" 하는 명령이 아니라 "어떻게 바꿀지" 를 담은 순수 함수 `A → A` 로 적고, 그 함수를 원자적으로 적용하는 데 있습니다.…

  - 30.1 이 장에서 다루는 것 — 상태 변화를 순수 함수로
  - 30.2 왜 필요한가 — 락 없는 공유 증가가 갱신을 잃습니다
  - 30.3 Atom\<A\> — 원자적 참조
  - 30.4 CAS 루프 직접 구현 — 읽고, 만들고, 갈아끼우고, 충돌하면 다시
  - 30.5 왜 순수 함수라야 하나 — 재시도의 안전성
  - 30.6 동시 증가 무손실 — payoff
  - 30.7 법칙으로 다지기 — 순차 정확성과 동시 무손실
  - 30.8 Elevated World 어휘로 다시 읽기
  - 30.9 Q&A — 자기 점검
  - 30.10 요약
  - 30.11 직접 해보기
  - 30.12 다음 장으로 — 여러 참조를 한 트랜잭션으로

### 31장. STM 과 Ref — 여러 참조를 한 트랜잭션으로 (상태 변화를 순수 함수로)
- **목표** — 이 장을 마치면 여러 개의 공유 참조를 하나의 트랜잭션으로 묶어, 그 안의 모든 변경이 함께 적용되거나 하나도 적용되지 않는 (all-or-nothing) 단위로 다룰 수 있습니다. 그 참조가 `Ref<A>` 이고, 트랜잭션이 `STM<A>` 라는 값임을 직접 구현으로 확인합니다. 앞…

  - 31.1 이 장에서 다루는 것 — 여러 참조를 함께 바꾸기
  - 31.2 왜 필요한가 — 두 Atom 으로 이체하면 중간 상태가 샙니다
  - 31.3 Ref<A> 와 STM<A> — 트랜잭션 참조와 트랜잭션 모나드
  - 31.4 STM 은 Monad — LINQ 로 트랜잭션 조립
  - 31.5 Atomically — 낙관적 커밋과 재시도
  - 31.6 동시 이체에도 총액이 보존됩니다
  - 31.7 법칙으로 다지기 — 단일 이체와 동시 보존
  - 31.8 더 깊이 — v5 의 STM 은 더 정교합니다
  - 31.9 Elevated World 어휘로 다시 읽기
  - 31.10 Q&A — 자기 점검
  - 31.11 요약
  - 31.12 다음 장으로 — 컬렉션 전체를 동시에

### 32장. 동시 컬렉션과 인과성 (AtomHashMap 과 VectorClock)
- **목표** — 이 장을 마치면 여러 스레드가 한 맵에 동시에 쓸 때도 항목을 잃지 않는 `AtomHashMap` 이 30장 `Atom` 의 CAS 를 불변 맵에 그대로 입힌 형태임을 직접 구현으로 확인할 수 있습니다. 락 하나 없이 8000 개의 동시 삽입이 어떻게 무손실로 끝나는지를 충돌과 재시도의…

  - 32.1 이 장에서 다루는 것 — Atom 을 컬렉션에, 그리고 인과성
  - 32.2 왜 필요한가 — 가변 Dictionary 동시 삽입은 깨집니다
  - 32.3 AtomHashMap — 불변 맵 + CAS
  - 32.4 VectorClock — 인과성 (부분 순서)
  - 32.5 송수신 시나리오 — happens-before 가 저절로 잡힌다
  - 32.6 법칙으로 다지기
  - 32.7 더 깊이 — v5 의 실제 모양과 분산에서의 자리
  - 32.8 Elevated World 어휘로 다시 읽기
  - 32.9 Q&A — 자기 점검
  - 32.10 요약
  - 32.11 다음 장으로 — 9부를 닫으며

## Part 10 — Streaming (스트리밍)

### 33장. StreamT — 효과를 품은 lazy 스트림 (당길 때만 계산되는 무한 시퀀스)
- **목표** — 이 장을 마치면 한 조각을 당길 때마다 (Pull) 계산이 일어나는 효과적 스트림 `StreamT<A>` 를 직접 구현할 수 있습니다. 핵심은 머리 (Head) 와 나머지 (Tail) 를 잇되 나머지를 `Func` 로 잠가 두어, 당기기 전에는 아무것도 계산하지 않는 데 있습니다. 4…

  - 33.1 이 장에서 다루는 것 — 효과를 품은 lazy 시퀀스
  - 33.2 왜 필요한가 — 무한·대용량을 컬렉션으로 들면 터집니다
  - 33.3 StreamT\<A\> — Cell 과 Pull
  - 33.4 Pull 기반 — 당길 때만 계산
  - 33.5 Map / Filter / Take 합성 — 한 조각씩
  - 33.6 효과는 당긴 만큼만 — payoff
  - 33.7 무한 소수 체 — 게으른 평가의 진수
  - 33.8 검사로 다지기 — 종료성·합성·당김 횟수
  - 33.9 더 깊이 — 학습용 StreamT 는 v5 의 단순화판입니다
  - 33.10 Elevated World 어휘로 다시 읽기
  - 33.11 Q&A — 자기 점검
  - 33.12 요약
  - 33.13 직접 해보기
  - 33.14 다음 장으로 — 생산·소비·변환을 세 역할로

### 34장. Pipes — 스트리밍을 세 역할의 합성으로 (Producer · Consumer · Pipe)
- **목표** — 이 장을 마치면 데이터 스트림을 한 타입이 아니라 세 역할의 조합으로 나누어 직접 구현할 수 있습니다. 값을 한 조각씩 내놓는 `Producer<O>` (생산), 받은 조각을 변환하는 `Pipe<I, O>` (변환), 흘러온 조각을 접어 결과를 내는 `Consumer<I, R>` (소비…

  - 34.1 이 장에서 다루는 것 — 스트리밍을 세 역할의 조합으로
  - 34.2 왜 필요한가 — 한 덩어리 변환은 갈아끼우기 어렵습니다
  - 34.3 Producer / Consumer / Pipe — 세 시그니처
    - 34.3.1 Producer — 당기면 한 조각을 내놓는 원천
    - 34.3.2 Consumer — 끝까지 당겨 한 결과로 접는 소비자
    - 34.3.3 Pipe — 흐르는 조각을 바꾸는 변환기
  - 34.4 합성 — producer | pipe | consumer
    - 34.4.1 Through 와 Run — 한 줄로 잇는 파이프라인
    - 34.4.2 Then — 변환끼리 이어 붙이기
  - 34.5 당김과 역압 — payoff
  - 34.6 단어 수 세기 — WordCount 챌린지
  - 34.7 법칙으로 다지기 — 합성 정합성과 역압
  - 34.8 더 깊이 (처음엔 건너뛰어도 됩니다) — v5 의 Pipes 와 학습용의 단순화
  - 34.9 Elevated World 어휘로 다시 읽기
  - 34.10 Q&A — 자기 점검
  - 34.11 요약
  - 34.12 직접 해보기
  - 34.13 다음 장으로 — 세 역할을 한 변환관으로, 자원까지 안전하게

### 35장. Conduit 와 실전 파이프라인 (자원 안전한 스트림 ETL)
- **목표** — 이 장을 마치면 입력 스트림을 출력 스트림으로 바꾸는 변환관 `Conduit<I, O>` 를 직접 구현하고, 앞 장의 Pipes 와 견주어 어느 자리에 무엇을 쓸지 고를 수 있습니다. 핵심은 Pipes 가 생산·변환·소비 세 역할로 나뉘던 자리를, Conduit 은 변환 한 덩어리 (`…

  - 35.1 이 장에서 다루는 것 — 또 다른 스트리밍 추상, 그리고 실전
  - 35.2 왜 필요한가 — 세 역할이 늘 필요치는 않고, 자원은 안 닫으면 샙니다
  - 35.3 Conduit\<I, O\> — IEnumerable 변환관
  - 35.4 Pipes vs Conduit — 어느 자리에 무엇
  - 35.5 자원 안전 파이프라인 — bracket 과 결합
  - 35.6 ETL payoff — 파싱 · 임계값 · 합산을 한 자리에
  - 35.7 법칙으로 다지기 — 합성 정합과 자원 보장
  - 35.8 더 깊이 — 학습용 Conduit 은 v5 의 단순화판입니다
  - 35.9 Elevated World 어휘로 다시 읽기
  - 35.10 Q&A — 자기 점검
  - 35.11 요약
  - 35.12 직접 해보기
  - 35.13 다음 부로 — 이제 이 효과·스트리밍 코드를 어떻게 테스트하는가

## Part 11 — Functional Testing (효과·전문성 테스트)

### 36장. 효과 코드의 결정적 테스트 (런타임을 바꿔 끼우는 테스트 더블)
- **목표** — 11부의 첫 장으로, 부수 효과가 섞인 코드를 실제 부수 효과 없이 검사하는 길을 엽니다. 이 장을 읽고 나면 7부에서 만든 `Eff<RT>` 가 왜 테스트하기 좋은 모양인지 시그니처로 설명하고, 운영 런타임 대신 `MemoryConsole` 테스트 더블을 주입해 콘솔이 섞인 효과 코드…

  - 36.1 1장 비유로 출발 — 효과 테스트는 Elevated World 의 어느 자리인가
    - 36.1.1 7부에서 만든 `Eff<RT>` 자리 복습
    - 36.1.2 효과는 값, 런타임은 의존성 — 이 장의 1장 매핑
  - 36.2 왜 필요한가 — 실제 콘솔에 묶인 테스트
  - 36.3 효과가 값이고 런타임이 의존성이다
    - 36.3.1 콘솔 능력을 인터페이스로 — `IConsole`
    - 36.3.2 런타임이 능력을 쥔다 — `Has<RT, IConsole>` 와 `AppRT`
    - 36.3.3 능력 기반 효과 — `Eff.WriteLine` 과 `Eff.ReadLine`
  - 36.4 `MemoryConsole` 더블 주입 — `Greeter.Ask` walkthrough
    - 36.4.1 테스트 더블 `MemoryConsole`
    - 36.4.2 테스트 대상 효과 — `Greeter.Ask`
    - 36.4.3 더블을 주입해 실행하기
  - 36.5 효과 테스트 작성 — `EffectTests`
    - 36.5.1 출력이 정확한가
    - 36.5.2 입력이 바뀌면 출력이 바뀌는가
    - 36.5.3 결정적인가
  - 36.6 7부 `Has<RT, Trait>` DI 가 테스트 용이성으로 이어진다
    - 36.6.1 실제 LanguageExt 와의 1:1 대응
  - 36.7 직접 해보기 — 챌린지
    - 36.7.1 빈 입력으로 실행해 보기
    - 36.7.2 새 능력 추가 — `IClock` 으로 시각 더블 끼우기
    - 36.7.3 `bool` 헬퍼를 `[Fact]` 로 옮기기
    - 36.7.4 세 챌린지가 노리는 능력
  - 36.8 Elevated World 어휘로 다시 읽기
  - 36.9 Q&A — 자기 점검
  - 36.10 요약
  - 36.11 다음 장으로 — 마무리 (37장 동시·스트리밍 다리)

### 37장. 동시 · 스트리밍 · 자원 효과 테스트 (비결정에서 결정적 한 조각을 끌어내려 단언)
- **목표** — 이 장을 읽고 나면 비결정성과 시간과 수명이 얽힌 동시 · 스트리밍 · 자원 효과를 결정적으로 검사할 수 있습니다. 원칙은 하나입니다. 비결정인 부분은 단언하지 않고, 거기서 끌어내린 결정적 한 조각만 단언합니다. 1000개 스레드가 동시에 증가시킨 카운터가 항상 정확히 1000 임을…

  - 37.1 1장 비유로 출발 — 비결정 효과는 Elevated World 의 어느 자리인가
    - 37.1.1 9 ~ 10부에서 만든 동시 · 스트리밍 효과 복습
    - 37.1.2 비결정 효과의 1장 매핑 — 단언은 끌어내린 결정적 사실
  - 37.2 왜 필요한가 — 실제 동작에 묶인 테스트
  - 37.3 결정적 불변식 — 순서는 비결정, 최종값은 결정
  - 37.4 `Atom` 원자성 — 1000개 동시 증가가 항상 1000
    - 37.4.1 비결정 순서가 결정적 최종값으로
    - 37.4.2 세 가지 검증 — 순차 · Parallel · Task
  - 37.5 `Schedule` 재시도 — 시간 없이 시도 횟수로
    - 37.5.1 네 가지 시도 횟수 단언
  - 37.6 `Resource` bracket — 예외에도 해제, 중첩은 LIFO
    - 37.6.1 세 가지 수명 단언
  - 37.7 스트림 골든 테스트 — 생성 시퀀스와 lazy 조기 정지
    - 37.7.1 세 가지 스트림 단언
    - 37.7.2 데모로 보는 네 도구
  - 37.8 직접 해보기 — 챌린지
    - 37.8.1 비원자 카운터로 잃어버린 갱신 관찰하기
    - 37.8.2 재시도에 백오프 로그를 더하기
    - 37.8.3 세 자원의 LIFO 순서 예측하기
    - 37.8.4 세 챌린지가 노리는 능력
  - 37.9 Elevated World 어휘로 다시 읽기
  - 37.10 Q&A — 자기 점검
  - 37.11 요약
  - 37.12 다음 장으로 — 마무리 (38장 property-based 다리)

### 38장. property-based 심화와 함수형 테스트 아키텍처 (씨앗에서 한 모듈로)
- **목표** — 이 장을 마치면 3장에 심은 한 줄짜리 `ForAll` 을 무작위 생성기와 축소 (shrinking) 로 키워, 손으로 고른 몇 개가 아니라 임의 입력 200 건으로 성질을 검사하고 실패하면 가장 작은 반례까지 줄일 수 있습니다. 나아가 기초 각 장에 흩어져 있던 법칙 검증을 어떤 `F…

  - 38.1 왜 필요한가 — 특정 값 몇 개로는 부족하다
    - 38.1.1 3장에서 심은 씨앗 복습
    - 38.1.2 특정 값 vs 임의 입력 — 입력 공간으로 보기
  - 38.2 생성기 `Gen` — 무작위 입력을 만드는 Elevated 값
    - 38.2.1 `Gen<A>` 의 정의
    - 38.2.2 `Gen` 은 사실 Functor
  - 38.3 `Prop.ForAll` 과 축소 — 최소 반례까지 줄이기
    - 38.3.1 검사 결과를 기록으로 — `Result<A>`
    - 38.3.2 축소의 규칙 — 더 작은데 여전히 실패하면 채택
    - 38.3.3 최소 반례 찾기 — `n < 100` 의 경계
    - 38.3.4 참인 성질 — 200 케이스 통과
  - 38.4 어떤 `F` 든 받는 법칙 모듈 — `Laws`
    - 38.4.1 1부 trait 의 toy 재현
    - 38.4.2 법칙 모듈의 시그니처 — generic + probe
    - 38.4.3 다섯 법칙을 한 모듈로
    - 38.4.4 법칙 검증이 위반을 실제로 잡는가 — `BogusListF`
  - 38.5 결합 — 임의 입력으로 법칙을 자동 검증
    - 38.5.1 두 축이 서로의 빈자리를 채운다
    - 38.5.2 임의 `MyList` 마다 법칙을 검사
  - 38.6 실무로 — CsCheck / FsCheck 이행
  - 38.7 직접 해보기 — 챌린지
    - 38.7.1 정렬의 성질을 property 로 검증하기
    - 38.7.2 법칙 위반 인스턴스를 같은 모듈로 잡기
    - 38.7.3 두 챌린지가 노리는 능력
  - 38.8 Elevated World 어휘로 다시 읽기
  - 38.9 Q&A — 자기 점검
  - 38.10 요약
  - 38.11 11부를 마치며 — 효과에서 성질까지

## Part 12 — Real-world (실무 예제 — 실전 적용)

### 39장. 도메인 모델링과 검증 — 잘못된 상태를 표현 불가능하게 (강타입 도메인 + Validation 누적 파이프라인)
- **목표** — 이 장을 마치면 잘못된 값이 아예 컴파일되지 않는 강타입 도메인을 설계하고, 3부에서 만든 `Validation` 의 applicative 누적으로 원시 입력 (`string` · `int`) 을 검증된 도메인 타입으로 끌어올리는 end-to-end 파이프라인을 작성할 수 있습니다. 핵…

  - 39.1 이 장에서 다루는 것 — 도메인을 타입으로
  - 39.2 왜 필요한가 — 원시 타입과 첫 오류에서 멈춤
  - 39.3 강타입 도메인 — 스마트 생성자
  - 39.4 Validation 으로 모든 오류 누적
  - 39.5 end-to-end payoff — Register 파이프라인
  - 39.6 법칙으로 다지기 — 누적의 핵심 성질
  - 39.7 더 깊이 — 학습용 Validation 은 v5 의 단순화판입니다
  - 39.8 Elevated World 어휘로 다시 읽기
  - 39.9 Q&A — 자기 점검
  - 39.10 요약
  - 39.11 다음 장으로 — 검증된 값으로 부수 효과를 일으키되 테스트 가능하게

### 40장. 효과 기반 애플리케이션 (Eff\<RT\> + Has DI 로 부수 효과 격리)
- **목표** — 이 장을 마치면 콘솔 / 시계 / 저장소 같은 여러 부수 효과를 능력 인터페이스에 의존하게 만들고, 그 능력들을 런타임 하나로 주입해 같은 앱 코드를 테스트용 런타임과 실제용 런타임으로 갈아 끼울 수 있습니다. 핵심은 효과 코드가 구현 (진짜 콘솔인지 인메모리 더블인지) 을 모른 채 `…

  - 40.1 이 장에서 다루는 것 — 부수 효과를 격리한 앱
  - 40.2 왜 필요한가 — 하드코딩한 효과는 테스트 불가
  - 40.3 능력 인터페이스 + 런타임 — 무엇을 할지와 누가 할지의 분리
  - 40.4 효과 워크플로 — LINQ 로 능력 조합
  - 40.5 두 런타임 — payoff
  - 40.6 검사로 다지기 — 결정성
  - 40.7 더 깊이 — 학습용 Has·런타임은 v5 의 단순화판입니다
  - 40.8 Elevated World 어휘로 다시 읽기
  - 40.9 Q&A — 자기 점검
  - 40.10 요약
  - 40.11 다음 장으로 — 대용량·동시 데이터 흐름을 한 파이프라인으로

### 41장. 동시성·스트리밍 실전 파이프라인 (네 Part 를 한 파이프라인으로)
- **목표** — 이 장을 마치면 따로 익힌 네 도구를 한 데이터 파이프라인 함수로 결합할 수 있습니다. 8부에서 만든 재시도 (Schedule) 로 불안정한 연결을 복구하고, 8부의 `bracket` (Resource) 으로 연 자원을 예외에도 반드시 닫고, 10부의 lazy 스트리밍으로 입력을 한 조…

  - 41.1 이 장에서 다루는 것 — 네 Part 를 한 파이프라인으로
  - 41.2 왜 필요한가 — 실무 파이프라인은 한 도구로 안 됩니다
  - 41.3 네 단계를 한 함수에 — Pipeline.Run
  - 41.4 payoff — 네 도구가 한 Report 로
  - 41.5 법칙으로 다지기 — 네 약속의 동시 성립
  - 41.6 더 깊이 — 학습용 결합은 v5 의 단순화판입니다
  - 41.7 Elevated World 어휘로 다시 읽기
  - 41.8 Q&A — 자기 점검
  - 41.9 요약
  - 41.10 직접 해보기
  - 41.11 다음 장으로 — 부분들을 한 서비스로

### 42장. 종합 capstone — 검증·효과·테스트가 한 서비스로 (책을 마치며)
- **목표** — 이 장을 마치면 따로 익힌 도구들을 주문 접수 서비스 한 편으로 합성할 수 있습니다. 3부에서 만든 `Validation` 의 누적 검증으로 입력을 도메인 타입으로 끌어올리고, 7부에서 만든 `Eff<RT>` + `Has<RT, TRAIT>` 능력 기반 DI 로 저장과 로그라는 부수 효…

  - 42.1 이 장에서 다루는 것 — 전부를 한 편으로
  - 42.2 왜 한 편으로 모으나 — 도구는 따로 배웠지만 실무는 함께 쓴다
  - 42.3 OrderService — 검증이 효과 분기를 정한다
  - 42.4 테스트 더블로 검증 — payoff
  - 42.5 책을 마치며 — 손으로 만든 패턴, v5 Samples 의 변형
  - 42.6 검증으로 다지기 — 승인·매출·로그
  - 42.7 더 깊이 — 학습용 capstone 은 v5 의 단순화판입니다
  - 42.8 Elevated World 어휘로 다시 읽기
  - 42.9 Q&A — 자기 점검
  - 42.10 요약
  - 42.11 이 책을 마치며
